# Server configuration
PORT=5000
NODE_ENV=development

# Database connection
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=lainaaminen

# JWT secret for authentication
JWT_SECRET=supersecretjwtkey

# CORS and client origin
CLIENT_URL=http://localhost:5000
