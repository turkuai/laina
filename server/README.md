# Dev mode

Run the server using `npm run dev` to enable the live reloading. Thus the server will automatically reload on changes in the source files and developers do not need to terminate it and start it manually after each change.
